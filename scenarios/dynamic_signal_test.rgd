# dynamic_signal_test.rgd
# 動的シグナルシステムのテストシナリオ

# キャラクター定義
character narrator "ナレーター" color=#ffffff

label dynamic_signal_test_start:

narrator "動的シグナルシステムのテストを開始します。"

# 1. 基本的な動的シグナル対応コマンドをテスト
narrator "まず、動的シグナル対応のコマンドをテスト..."

# Window Command（動的シグナル経由）
window shake intensity=2.0 duration=0.5

wait 1.0

# Screen Flash Command（動的シグナル経由）
screen_flash color=red duration=0.3

wait 1.0

# Camera Shake Command（動的シグナル経由）
camera_shake intensity=1.5 duration=0.8 type=horizontal

wait 1.0

narrator "基本的な動的シグナル対応コマンドが動作しました。"

wait 1.0

# 2. パーティクルエフェクトをテスト
narrator "次に、パーティクルエフェクトをテスト..."

particles sparkle intensity=high duration=3.0

wait 2.0

narrator "全ての動的シグナルテストが完了しました！"

wait 2.0

narrator "システムは正常に動作しています。"

# テスト終了
return